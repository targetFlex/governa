# Report — Sessão 2.66 · Incidente de branch de produção corrigido

**Data:** 2026-07-16
**Status:** ✅ concluída
**Sessão anterior:** [`2026-07-15-sessao-2.65.md`](./2026-07-15-sessao-2.65.md)

## TL;DR

`governa-core` no Railway estava deployando de `feat/session-2.1-protheus-auth` (PR #1, aberto desde 31/05, nunca mergeado) em vez de `main` — um fix de tracer aplicado direto nessa branch derrubou o login em produção. Branch de produção repontada para `main`; `governa-ui` reconectado ao GitHub (auto-deploy restaurado).

## O que mudou no código

Nenhuma mudança de código nesta sessão — apenas configuração de infraestrutura (Railway).

## Estado verificado do sistema

- `governa-core` (Railway Settings → Source): repontado para `main`. Deploy `06857fab` SUCCESS.
- `governa-ui`: reconectado ao GitHub (`targetFlex/governa`, branch `main`, root directory `apps/governa-ui`). Deploy `02c1b9dc` SUCCESS.
- Login em produção confirmado funcionando novamente (JWT válido para `fabio@targetflex.com.br`).

## Decisões tomadas

- **D9** — branch de deploy do `governa-core` corrigida para `main` · a branch anterior (`feat/session-2.1-protheus-auth`) havia divergido de `main` desde 13/06 (faltavam 72 commits, incluindo E7 auth local e E3.1-E3.5 Agente Âncora); um fix de tracer aplicado direto nela causou auto-deploy que quebrou `/auth/login`.
- **D10** — `governa-ui` reconectado via `railway service source connect --repo <owner/repo> --branch <branch> --service <nome>` (CLI) em vez do dropdown do dashboard · o dropdown de seleção de repo no Railway é flaky via Claude-in-Chrome (abre aba de instalação do GitHub App em vez de selecionar, mesmo com permissões corretas).

## Itens em aberto (não bloqueantes)

- [x] Deletar branch órfã `fix/tracer-otel-ts-ignore` — resolvido na sessão 2.67
- [x] Revisar/fechar PR #1 — resolvido na sessão 2.67 (fechado sem merge, escopo original já em `main`)

## Próxima sessão (2.67) deve

- **Atacar:** iniciar E8 ou próxima feature do roadmap
- **Começar lendo:** este report (e SÓ este no boot)
- **Atenção a:** sempre conferir a branch de produção configurada no Railway (Settings → Source) bate com `main` antes de assumir que reconectar/redeployar é seguro

## Commits da sessão

Nenhum commit de código — apenas configuração via Railway dashboard/CLI.
