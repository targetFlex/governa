# Report — Sessão 2.11 — 2026-05-30

## TL;DR
Implementados `PedidosService`, `ClientesService` (NGRX Signals) e `DashboardComponent` com tabelas, skeleton loading, paginação e WCAG 2.1 AA. 20 specs criadas. Após execução no host, 3 falhas corrigidas: bug de fuso em `addYears`, `axios` ausente no pact consumer, typo `coverageThresholds` no jest config do governa-ui.

## Estado
Concluída

## O que foi feito
- `shared/models/pedido.model.ts`: tipos `Pedido`, `PedidoItem`, `PedidosResponse`, `StatusPedido`
- `shared/models/cliente.model.ts`: tipos `Cliente`, `ClientesResponse`, `TipoPessoa`
- `features/pedidos/pedidos.service.ts`: `PedidosStore` (signalStore) + `PedidosService` facade — `loadPedidos(page, pageSize)`, `clearError()`, computed `isEmpty`, `hasError`, `totalPages`
- `features/clientes/clientes.service.ts`: `ClientesStore` (signalStore) + `ClientesService` facade — `loadClientes(page, pageSize, filtro)`, `clearError()`, computed `isEmpty`, `hasError`, `totalPages`, `clientesAtivos`
- `features/dashboard/dashboard.component.ts`: reescrito com tabelas de pedidos e clientes, skeleton shimmer, paginação, status badges, aria-live, aria-labelledby, sr-only, tabindex, WCAG 2.1 AA
- `features/pedidos/pedidos.service.spec.ts`: 10 casos — estado inicial, loading, sucesso, erro 500, erro sem message, clearError, query params
- `features/clientes/clientes.service.spec.ts`: 10 casos — estado inicial, loading, sucesso, computed clientesAtivos, erro 500, erro sem message, clearError, query params filtro/sem filtro/espaços
- **Fix 1** — `audit.service.ts`: `addYears` migrado para `getUTCFullYear()` + `setUTCFullYear()` para evitar bug de fuso (UTC-3 virava ano antes)
- **Fix 2** — `audit.service.spec.ts`: assertion usa `getUTCFullYear()` — CI-safe em qualquer timezone
- **Fix 3** — `governa-core/package.json`: `axios ^1.7.2` adicionado em `devDependencies` (requerido pelo pact consumer spec)
- **Fix 4** — `governa-ui/jest.config.js`: typo `coverageThresholds` → `coverageThreshold`

## Decisões tomadas
- **D1** — Service facade sobre SignalStore: componentes injetam `PedidosService`/`ClientesService` via DI sem acoplamento ao token do store
- **D2** — `filtro` como parâmetro opcional em `loadClientes`: omitido do request quando vazio ou só espaços
- **D3** — `clientesAtivos` como computed signal no store: evita filtro no template, reatividade automática
- **D4** — `addYears` com UTC: comportamento determinístico independente do timezone do host/CI

## Desvios do plano
- `pnpm install` e `pnpm test` não puderam ser executados no sandbox (npm registry bloqueado) — executados no host pelo usuário, revelando 3 falhas que foram corrigidas na mesma sessão

## Handoff — próxima sessão 2.12
Implementar `GET /pedidos` e `GET /clientes` no `governa-gateway`: connector Protheus + rota Express + pact provider spec. Confirmar que `pnpm test` passa 100% após `pnpm install` (axios agora disponível).

## Arquivos modificados
- `apps/governa-ui/src/app/shared/models/pedido.model.ts` — novo
- `apps/governa-ui/src/app/shared/models/cliente.model.ts` — novo
- `apps/governa-ui/src/app/features/pedidos/pedidos.service.ts` — novo
- `apps/governa-ui/src/app/features/pedidos/pedidos.service.spec.ts` — novo (10 specs)
- `apps/governa-ui/src/app/features/clientes/clientes.service.ts` — novo
- `apps/governa-ui/src/app/features/clientes/clientes.service.spec.ts` — novo (10 specs)
- `apps/governa-ui/src/app/features/dashboard/dashboard.component.ts` — reescrito com tabelas
- `apps/governa-core/src/modules/audit/application/audit.service.ts` — fix addYears UTC
- `apps/governa-core/src/modules/audit/application/audit.service.spec.ts` — fix getUTCFullYear
- `apps/governa-core/package.json` — axios adicionado em devDependencies
- `apps/governa-ui/jest.config.js` — fix typo coverageThreshold

## Pendências / alertas para sessões futuras
- `git push` do fix commit deve ser feito no host após `rm .git/index.lock`
- `POST /auth/login` ainda pendente no gateway (vem de sessão anterior)
- Storybook + Axe-core + Playwright agendados para sessão futura
- `shared/` contém apenas models — `DataTableComponent` e `PaginationComponent` podem ser extraídos nas próximas sessões
