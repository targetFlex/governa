# Report — Sessão 2.14 — 2026-05-30

## TL;DR
Módulo `pedidos` criado no governa-core com domínio puro, porta `IGatewayClient` (hexagonal) e `ConsultarPedidoUseCase` orquestrando gateway + audit LGPD. 239 testes PASS (↑10 vs. 229), cobertura 96.7% stmts / 100% funcs. Commit `606eccd` no branch `feat/session-2.14-gateway-client`.

## Estado
Concluída

## O que foi feito
- Criado `src/modules/pedidos/domain/pedido.entity.ts`: `PedidoInterno`, `ItemPedido`, `StatusPedido` — entidades imutáveis sem dep do Protheus
- Criado `src/modules/clientes/domain/cliente.entity.ts`: `ClienteInterno` — PII como tokens HMAC, sem texto claro
- Criado `src/modules/pedidos/domain/pedido.errors.ts`: `PedidoNotFoundError` + `GatewayUnavailableError` (erros de domínio, desacoplados de HTTP)
- Criado `src/shared/ports/gateway-client.port.ts`: `IGatewayClient` — porta hexagonal com `consultarPedidos()` + `consultarClientes()`; `ConsultarPedidosParams` e `ConsultarClientesParams`
- Criado `src/modules/pedidos/application/consultar-pedido.use-case.ts`: `ConsultarPedidoUseCase` — busca via `IGatewayClient` → grava `AuditEvent` (outcome `EXECUTADO`/`ERRO`, dataCategories LGPD) → retorna pedidos; grava audit SEMPRE (sucesso ou falha)
- Criado `test/fixtures/in-memory-gateway-client.ts`: `InMemoryGatewayClient` — fake adapter com `seedPedidos()`, `seedClientes()`, `simulateUnavailable()` e `calls` spy
- Criado `src/modules/pedidos/application/consultar-pedido.use-case.spec.ts`: 10 specs TDD (CP-1 a CP-10): happy path, filtro por número, not found, lista vazia, gateway indisponível, audit SUCCESS, audit FAILURE, categorias LGPD, inputSummary sem PII, filtros propagados corretamente

## Decisões tomadas
- **D1** — `Outcome` usa `EXECUTADO`/`ERRO` (não `SUCCESS`/`FAILURE`): respeita o enum de domínio existente do governa-core
- **D2** — `inputSummary` construído por `buildSummary()`: serializa filtros sem expor PII (apenas números de pedido e IDs internos)
- **D3** — Audit gravado SEMPRE antes de re-lançar erro de gateway: rastreabilidade LGPD exige registro mesmo em falha técnica
- **D4** — Busca sem `numeroPedido` retorna `[]` sem erro; busca com `numeroPedido` sem resultado lança `PedidoNotFoundError`: distinção entre consulta exploratória e consulta específica

## Desvios do plano
- Nenhum

## Handoff — próxima sessão 2.15
Implementar `HttpGatewayClient` em `governa-core/src/shared/infrastructure/http-gateway-client.ts` — adaptador HTTP real que chama `governa-gateway` (`GET /pedidos`, `GET /clientes`) e injeta `IGatewayClient` no `ConsultarPedidoUseCase`. Validar com testes de integração usando servidor Express in-process (padrão já usado nas sessões 2.7–2.8).

## Arquivos modificados
- `apps/governa-core/src/modules/pedidos/domain/pedido.entity.ts` — novo
- `apps/governa-core/src/modules/pedidos/domain/pedido.errors.ts` — novo
- `apps/governa-core/src/modules/pedidos/application/consultar-pedido.use-case.ts` — novo
- `apps/governa-core/src/modules/pedidos/application/consultar-pedido.use-case.spec.ts` — novo
- `apps/governa-core/src/modules/clientes/domain/cliente.entity.ts` — novo
- `apps/governa-core/src/shared/ports/gateway-client.port.ts` — novo
- `apps/governa-core/test/fixtures/in-memory-gateway-client.ts` — novo

## Pendências / alertas para sessões futuras
- `HttpGatewayClient` (adaptador HTTP real) — principal pendência: sem ele o `ConsultarPedidoUseCase` não roda em produção
- `ConsultarClienteUseCase` — análogo ao `ConsultarPedidoUseCase`, ainda não criado
- Rota `GET /pedidos` no `governa-core` (router Express) — expõe o use-case para o frontend/UI
- Storybook + Axe-core + Playwright E2E ainda pendentes (governa-ui)
- CI/CD com rollback automático ainda não implementado
