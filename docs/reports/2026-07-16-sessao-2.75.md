# Report — Sessão 2.75 · Model ID fix + Fase 3 MCP (fundação de schema)

**Data:** 2026-07-16
**Status:** ✅ concluída
**Sessão anterior:** [`2026-07-16-sessao-2.74.md`](./2026-07-16-sessao-2.74.md)

## TL;DR

Tentativa de ativar o `AnchorAgentService` ficou bloqueada por falta de `ANTHROPIC_API_KEY`
(usuário ainda não tem); corrigido o model ID inválido no meio tempo. Sessão pivotou para a
Fase 3 do MVP2 (conectores MCP funcionais): `McpServerRef` ganhou `url`/`headers`, primeira de
7 sessões planejadas para entregar a Fase 3 inteira.

## O que mudou no código

- `anthropic-llm-adapter.ts` — `MODEL` corrigido de `claude-sonnet-4-6` (inválido) para
  `claude-sonnet-5`. `AnchorAgentService` continua desligado (sem API key).
- `agent.schemas.ts` / `agent-inventory.entity.ts` — `McpServerRef` ganha `url`/`headers`
  opcionais (transporte MCP remoto via Streamable HTTP; sem stdio — RCE inaceitável num
  backend multi-tenant). Liberado também em `UpdateAgentSchema`/`UpdateAgentInput`.
- `prisma-agent-inventory.repository.ts` — leitura de `mcpServers` passa a validar via Zod
  (`McpServerRefSchema.array().safeParse`) em vez de cast cego; shape inválido cai para `[]`
  com warning em vez de propagar lixo pro domínio.
- Bug encontrado e corrigido no caminho: `findAllForTenant()` fazia `agents.map(this.toEntity)`
  — perdeu o `this` binding assim que `toEntity` passou a chamar `this.parseMcpServers`; virou
  `agents.map(agent => this.toEntity(agent))`.
- Fixture `in-memory-agent-inventory.repository.ts` (testes) também não persistia `mcpServers`
  no `update()` — corrigido.
- Nenhuma migration Prisma — `mcp_servers` já é coluna `Json`.

## Estado verificado do sistema

- **Banco:** `prisma migrate deploy` rodado contra Postgres local (`docker compose up -d postgres`,
  container já existia de sessão anterior) — 6 migrations, todas aplicadas, nenhuma nova nesta sessão.
- **Testes:** 56 suites · 615 testes (todos verdes) · coverage 92.37% stmt / 84.38% branch /
  92.45% func / 92.68% lines — os 4 thresholds de 80% mantidos. `agent.schemas.ts` em 100%.
  Suíte de integração (`test:integration`, Postgres real) rodada especificamente para
  `agent-inventory` — 18/18 verdes, incluindo os 3 cenários novos de round-trip com `url`/`headers`.
- **Build:** `tsc --noEmit` limpo.

## Decisões tomadas

- **D27** — Ativação do `AnchorAgentService` fica pendente até o usuário ter uma
  `ANTHROPIC_API_KEY` real; não simular/mockar a ativação em produção sem a key.
- **D28** — Fase 3 (MCP) dividida em 7 sessões sequenciais (2.75–2.81), não uma tacada só —
  tamanho comparável às sessões já registradas do projeto (2.71/2.74). Plano completo salvo
  em `/Users/fabiogomes/.claude/plans/rippling-crafting-hammock.md`.
- **D29** — Transporte MCP: só HTTP remoto (Streamable HTTP) nesta fase. stdio (processo local)
  fica de fora — spawnar processo a partir de config de agente num backend multi-tenant é
  superfície de RCE inaceitável agora.
- **D30** — Credenciais de conector MCP (`headers`): campo simples no mesmo JSON do agente por
  ora, documentado como débito de segurança no schema. Cofre de secrets dedicado fica para depois.
- **D31** — `url` opcional em `McpServerRef` (não obrigatório) — agentes/templates existentes
  não têm `url` e não podem quebrar validação; sem `url` o conector continua sendo metadado
  descritivo (comportamento anterior preservado).

## Itens em aberto (não bloqueantes)

- [ ] `ANTHROPIC_API_KEY` — usuário ainda não tem. Ativação do `AnchorAgentService` (local e/ou
      produção/Railway) fica parada até então.
- [ ] Fase 3 (MCP): só a sessão 2.75 (schema) está feita. Faltam 2.76 (cliente MCP), 2.77
      (proveniência de tool), 2.78 (wiring no `AnchorAgentService`), 2.79 (auditoria), 2.80
      (editor de UI), 2.81 (hardening).
- [ ] `headers` de conector MCP em texto simples — débito de segurança documentado, resolver
      antes de conectar servidor MCP de terceiro real em produção.

## Próxima sessão (2.76) deve

- **Atacar:** cliente MCP runtime — instalar `@modelcontextprotocol/sdk` em `governa-core`,
  novo adapter `apps/governa-core/src/modules/anchor-agent/infrastructure/mcp-client-adapter.ts`
  (`StreamableHTTPClientTransport`, `tools/list` → `LlmToolDef[]`, `tools/call` → `ToolHandler`).
- **Começar lendo:** este report (e SÓ este no boot).
- **Consultar sob demanda:** `/Users/fabiogomes/.claude/plans/rippling-crafting-hammock.md`
  (plano completo da Fase 3, sessões 2.76–2.81 detalhadas) e
  `docs/GOVERNA_AGENTES_MVP_SOLUTION.md` §"MVP2 — Escopo Detalhado" (checklist oficial da Fase 3).
- **Atenção a:** nomear as tools MCP prefixadas por `serverId` (ex. `mcp__{serverId}__{toolName}`)
  para evitar colisão com tools nativas; testar o adapter mockando o `Client` do SDK (não precisa
  MCP server real rodando para a sessão 2.76).
- **Não tocar:** `AnchorAgentService`/`server.ts` (wiring é 2.78), `ToolScopeBuilder`/`Tool`
  (proveniência é 2.77), `AuditEvent` (é 2.79), UI (`governa-ui`, é 2.80).

## Commits da sessão

- `e793f5d` — fix(governa-core): corrigir model ID inválido no AnthropicLlmAdapter
- `3a92924` — feat(governa-core): estender McpServerRef com url/headers (Fase 3 MVP2, sessão 2.75)
