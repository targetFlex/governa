# Report — Sessão 2.61 · Infraestrutura — Cloudflare DNS + decisão de hosting

**Data:** 2026-07-10
**Status:** ✅ concluída
**Sessão anterior:** [`2026-07-05-sessao-2.60.md`](./2026-07-05-sessao-2.60.md)

## TL;DR

Domínio `aicockpit.com.br` ativado no Cloudflare (nameservers migrados da Locaweb), `www` configurado com proxy, e decisão de hosting MVP fechada em **Railway** (no lugar de VPS Hetzner).

## O que mudou no código

- Nenhuma mudança de código nesta sessão — trabalho exclusivamente de infraestrutura.

## Estado verificado do sistema

- **Cloudflare:** zona `aicockpit.com.br` status `active` desde 2026-07-08T23:32Z
  - Nameservers: `trace.ns.cloudflare.com` / `zariyah.ns.cloudflare.com`
  - Zone ID: `35253e7782d073a112bbbb7b8b81720f`
  - Plano: Free
- **DNS configurado:**
  - `www.aicockpit.com.br` → CNAME → `aicockpit.com.br` [proxied] ✅
- **DNS pendente (aguarda Railway):**
  - `aicockpit.com.br` (apex) → CNAME → `<frontend>.up.railway.app`
  - `api.aicockpit.com.br` → CNAME → `<governa-core>.up.railway.app`
  - `gateway.aicockpit.com.br` → CNAME → `<governa-gateway>.up.railway.app`
- **SSL/TLS:** não configurado via API (token atual não tem permissão de Zone Settings — só DNS:Edit)
- **MCP Cloudflare:** configurado em `~/.claude/settings.json` mas não carrega na sessão — reiniciar Claude Code para ativar

## Decisões tomadas

- **D1** — Hosting MVP: **Railway** (não Hetzner CPX21) — deploy mais rápido sem overhead de infra; migração para AWS planejada quando PMEs reais entrarem (S3 Object Lock para audit archive já previsto no Solution Doc)
- **D2** — Token DNS separado criado no Cloudflare (`Edit zone DNS` — apenas `aicockpit.com.br`) — não usar o token MCP original (só tem Worker + zone:read) para operações DNS
- **D3** — SSL/TLS e HTTPS redirect precisam de token com permissão `Zone Settings:Edit` — a configurar manualmente no painel ou via novo token

## Itens em aberto (não bloqueantes)

- [ ] SSL/TLS mode → configurar para `Full` no painel Cloudflare (Dashboard → SSL/TLS → Overview)
- [ ] Always Use HTTPS → ativar no painel (Dashboard → SSL/TLS → Edge Certificates)
- [ ] Token DNS salvo apenas na sessão — guardar em local seguro (1Password, etc.)
- [ ] MCP Cloudflare não carrega na sessão atual — reiniciar Claude Code

## Próxima sessão (2.62) deve

- **Atacar:** E7 real — criar conta Railway, conectar repositório `governa-ui` (primeiro serviço), obter CNAME Railway e configurar DNS apex + subdomínios
- **Sequência:**
  1. Criar conta em railway.app
  2. New Project → Deploy from GitHub → selecionar `governa-ui`
  3. Em Settings → Domains → Add Custom Domain → `aicockpit.com.br`
  4. Railway fornece CNAME → trazer para esta sessão, configurar DNS via API
  5. Repetir para `governa-core` (`api.`) e `governa-gateway` (`gateway.`)
- **Começar lendo:** este report (e SÓ este no boot)
- **Consultar sob demanda:** `docs/GOVERNA_AGENTES_MVP_SOLUTION.md` §Decisões Técnicas (stack e arquitetura)
- **Atenção a:** token DNS Cloudflare disponível em `~/.claude/settings.json` (chave `cloudflare`) — usar apenas para DNS, não tem permissão de Zone Settings
- **Não tocar:** código da aplicação — nenhuma alteração de código nesta sessão

## Commits da sessão

- n/a — sessão de infraestrutura, sem commits de código
