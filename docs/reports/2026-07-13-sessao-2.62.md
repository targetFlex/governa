# Report — Sessão 2.62 · E7 — governa-ui no Railway + aicockpit.com.br no ar

**Data:** 2026-07-13
**Status:** ✅ concluída
**Sessão anterior:** [`2026-07-10-sessao-2.61.md`](./2026-07-10-sessao-2.61.md)

## TL;DR

`governa-ui` deployado no Railway e acessível em `https://aicockpit.com.br` com Cloudflare proxy + SSL Full. Backend (core + gateway) ainda não deployado.

## O que mudou no código

- `apps/governa-ui/Dockerfile` — build multi-stage node:22-alpine → nginx:1.27-alpine; usa `envsubst` para injetar `$PORT` do Railway
- `apps/governa-ui/nginx.conf` — serve SPA Angular com HTML5 routing; porta via `${PORT}`
- `apps/governa-ui/.dockerignore` — exclui node_modules, dist, coverage do contexto Docker

## Estado verificado do sistema

- **Testes:** 32 suites · 571 testes · branches 81.85% (sem alteração de código)
- **Build:** `ng build` production OK dentro do Docker
- **Deploy:** `governa-ui` ativo no Railway — `https://governa-ui-production.up.railway.app` → 200
- **Domínio:** `https://aicockpit.com.br` → 200 ✅ (Cloudflare proxy + SSL Full + Railway cert Let's Encrypt)
- **governa-core / governa-gateway:** não deployados — sem URL pública

## Decisões tomadas

- **D1** — Railway (não Hetzner) confirmado como plataforma MVP — vem da sessão 2.61, executado aqui
- **D2** — Dockerfile auto-contido em `apps/governa-ui/` (sem contexto de monorepo) — governa-ui não tem dependências internas de workspace
- **D3** — nginx usa template `envsubst` (`/etc/nginx/templates/`) para injetar `PORT` do Railway — sem PORT fixo hardcoded
- **D4** — `targetPort: null` no domínio customizado Railway — deixar auto-detecção; `targetPort: 80` causava 502 pois Railway injeta PORT em valor diferente de 80
- **D5** — Proxy Cloudflare desativado temporariamente para validação Railway, reativado após cert emitido — Railway não consegue verificar CNAME com proxy ativo
- **D6** — `www.aicockpit.com.br` deixado com 404 por ora — plano free Railway limita 1 domínio customizado por serviço; resolver via Redirect Rule Cloudflare em sessão futura
- **D7** — Token Cloudflare DNS:Edit (`cfut_IXvu0F…`) atualizado em `~/.claude/settings.json` (escopo: DNS:Edit para `aicockpit.com.br`)

## Itens em aberto (não bloqueantes)

- [ ] `www.aicockpit.com.br` → 404 (limite plano free Railway; resolver com Redirect Rule no Cloudflare)
- [ ] `governa-core` e `governa-gateway` não deployados — login na UI falha (sem API)
- [ ] Endpoint `POST /auth/login` não existe no gateway — tela de login não funcional
- [ ] Nenhum utilizador seed — mesmo com gateway deployado, sem credencial para logar
- [ ] SSL/TLS Cloudflare alterado para Full manualmente — token atual (DNS:Edit) não tem Zone Settings:Edit

## Próxima sessão (2.63) deve

- **Atacar:** E7 continuação — deploy de `governa-core` no Railway + endpoint `POST /auth/login` no gateway + utilizador seed para acesso inicial
- **Sequência sugerida:**
  1. Criar serviço `governa-core` no Railway + PostgreSQL add-on
  2. Criar Dockerfile para governa-core (já existe em `apps/governa-core/Dockerfile`)
  3. Configurar env vars no Railway (JWT_SECRET, DATABASE_URL, etc.)
  4. Implementar `POST /auth/login` no gateway (email + senha → JWT com tenantId + userId)
  5. Seed de utilizador `fabio@targetflex.com.br` / senha temporária
  6. Testar login em `aicockpit.com.br/login`
- **Começar lendo:** este report (e SÓ este no boot)
- **Consultar sob demanda:** `apps/governa-core/Dockerfile` (existe), `apps/governa-core/prisma/seed.ts` (tenant já criado, sem user)
- **Atenção a:** Railway free plan tem limites — PostgreSQL add-on pode precisar de plano pago; avaliar usar Railway PostgreSQL vs Supabase free tier
- **Não tocar:** `governa-ui` — está estável e deployado; não alterar Dockerfile/nginx sem necessidade

## Commits da sessão

- `fc071bb` — feat(E7): Dockerfile + nginx para deploy governa-ui no Railway
