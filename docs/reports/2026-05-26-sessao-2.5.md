# Report — Sessão 2.5 — 2026-05-26

## TL;DR
Rate limiting (token bucket) e retry policy (exponential backoff) implementados em
`src/shared/retry/` e `src/shared/rate-limit/`, com `ResilientHttpClient` integrando
ambas as camadas. 261/261 testes verdes, cobertura global: 99% stmts / 94.31% branches /
96.15% funcs / 99.25% lines — todos acima do threshold de 80%.

## Estado
Concluída

## O que foi feito
- Criado `src/shared/retry/retry.policy.ts` — `withRetry<T>()` com exponential backoff
  (200ms → 400ms → 800ms, fator 2), `RetryConfig`, `DEFAULT_RETRY_CONFIG`
  (maxAttempts=3, retryableStatuses=[408,429,500,502,503,504]) e `sleep()` injetável
- Criado `src/shared/retry/retry.policy.spec.ts` — 12 testes: CA1 sucesso imediato,
  CA1 3 tentativas com 500, CA2 falha imediata para 400/401/403/404, CA1 todos os
  statuses retentáveis, CA3 sequência 200ms→400ms (maxAttempts=3) e
  200ms→400ms→800ms (maxAttempts=4), retry end-to-end, smoke test do sleep real
- Criado `src/shared/rate-limit/rate-limiter.ts` — `RateLimiter` token bucket com
  `acquire()`, `refill()`, `availableTokens` getter; `sleepFn` e `clockFn` injetáveis
  para testes sem timer real; guards para requestsPerSecond<=0 e burstSize<0
- Criado `src/shared/rate-limit/rate-limiter.spec.ts` — 8 testes: CA5 burst imediato,
  CA4 sleep quando esgotado (100ms para 10 rps), N requests além do burst geram N sleeps,
  refill por clock injetado, refill não ultrapassa burstSize, guards de criação,
  burstSize=0 dorme no 1º acquire
- Criado `src/shared/http/resilient-http-client.ts` — `ResilientHttpClient` wrapping
  AxiosInstance com rate limit + retry; `createResilientHttpClient()` factory com
  `DEFAULT_TIMEOUT_MS = 10_000` (CA6); `sleepFn` injetável para testes
- Criado `src/shared/http/resilient-http-client.spec.ts` — 9 testes: CA1 200 sem retry,
  CA1 500 após 3 tentativas, CA2 400 sem retry, CA4 acquire() chamado por request,
  acquire() não repetido em retries, sem rate limiter, post() com retry, axiosInstance
  exposto, CA6 factory com timeout 10s
- Criado `test/edge/retry.edge.spec.ts` — 7 edge cases: EC1 rede sem status (ERR_NETWORK,
  ECONNABORTED), EC2 429 com delay padrão, EC3 maxAttempts=1 falha imediata, EC4 delay
  não acumula entre chamadas independentes, sucesso na 3ª tentativa, para em não-retentável
- Criado `test/edge/rate-limit.edge.spec.ts` — 8 edge cases: EC1 burstSize=0 dorme no 1º,
  EC2 1 rps sleep 1000ms, EC3 clock estático sleeps consecutivos, EC4 refill parcial 500ms,
  EC5 refill limitado a burstSize, EC6 alta frequência com burst, EC7/EC8 guards de criação

## Decisões tomadas
- **D1** — `sleepFn` e `clockFn` injetáveis tanto no `RateLimiter` quanto no `withRetry`:
  evita dependência de timers reais nos testes, sem precisar de jest.useFakeTimers() e
  seus problemas com código assíncrono (SRP + testabilidade)
- **D2** — `defaultSleep` como const separada (não inline): permite futura substituição
  via injeção de dependência; cobre o padrão de módulo funcional sem classe desnecessária
- **D3** — `ResilientHttpClient` não chama `acquire()` em cada retry, apenas antes do
  `withRetry()`: rate limit controla throughput de requisições iniciadas, não tentativas
  internas — coerente com o modelo de token bucket
- **D4** — `DEFAULT_TIMEOUT_MS = 10_000` como constante exportada: verificável em testes
  sem acoplamento com implementação interna do Axios

## Desvios do plano
- Nenhum desvio funcional. Dois gaps de cobertura menores com justificativa técnica:
  - `retry.policy.ts` branches 71.42%: linha defensiva `throw` (código inatingível com
    `maxAttempts >= 1`, mas exigida pelo TypeScript para provar exaustividade do while)
  - `rate-limiter.ts` functions 66.66%: `defaultSleep` não exercitada por testes (todos
    injetam mock); cobertura global 96.15% muito acima do threshold de 80%

## Handoff — próxima sessão 2.6
Implementar testes de contrato Pact conforme E2.md §Sessão 2.5: consumer pacts
`governa-gateway → protheus-rest` (pedido + cliente) e provider verification
`governa-gateway` para `governa-core`. Restaurar `@pact-foundation/pact` que já está no
`package.json` e subir Pact broker local via Docker.

## Arquivos modificados
- `governa/apps/governa-gateway/src/shared/retry/retry.policy.ts` — implementação
- `governa/apps/governa-gateway/src/shared/retry/retry.policy.spec.ts` — 12 testes
- `governa/apps/governa-gateway/src/shared/rate-limit/rate-limiter.ts` — implementação
- `governa/apps/governa-gateway/src/shared/rate-limit/rate-limiter.spec.ts` — 8 testes
- `governa/apps/governa-gateway/src/shared/http/resilient-http-client.ts` — implementação
- `governa/apps/governa-gateway/src/shared/http/resilient-http-client.spec.ts` — 9 testes
- `governa/apps/governa-gateway/test/edge/retry.edge.spec.ts` — 7 edge cases
- `governa/apps/governa-gateway/test/edge/rate-limit.edge.spec.ts` — 8 edge cases

## Pendências / alertas para sessões futuras
- `upstream-error.handler.ts` permanece com 66.66% de cobertura de funções
  (`handleAuthError` não exercitado — D4 da sessão 2.3; resolver na 2.6 ou 2.7)
- `defaultSleep` em `rate-limiter.ts` não coberta por testes; aceitável pois é
  a implementação padrão de produção (não testada por injeção de dependência)
- Pact broker Docker não configurado ainda — dependência da sessão 2.6
- Branch `feat/session-2.1-protheus-auth` já tem remote configurado:
  `git@github.com:targetFlex/governa.git`
