# Report — Sessão 2.46 · Wire NotificationService + E5 concluído → decisão E3 vs E4

**Data:** 2026-06-28
**Status:** 🔄 a iniciar
**Sessão anterior:** [`2026-06-28-sessao-2.45.md`](./2026-06-28-sessao-2.45.md)

---

## Contexto de retomada

### O que a sessão 2.45 entregou

- `NotificationConfig` completamente implementado (model, migration SQL, domain, infra, application, presentation)
- `NotificationService` — SMTP (nodemailer) + webhook HMAC-SHA256 — código pronto e testado (473 testes, CI verde)
- **Pendência bloqueante:** `NotificationService` ainda **não está wired** em `server.ts`; o router `/notifications` só monta se `deps.notificationService` for passado (opt-in no `createApp`)
- `_tmp_*` files e `skills/ziJ8B7ij` continuam não commitados (baixa prioridade)

### Estado verificado do repo

```
Branch atual: feat/session-2.45-notification-config
Testes: 473 passando, 45 suites — CI verde
Typecheck: limpo
Docker/banco: não disponível na sessão 2.45 — migration SQL pronta
```

### Arquivos relevantes para esta sessão

| Arquivo | Papel |
|---|---|
| `apps/governa-core/src/server.ts` | Bootstrap — onde `NotificationService` deve ser wired |
| `apps/governa-core/src/app.ts` | Factory do Express — já importa e aceita `notificationService?` |
| `apps/governa-core/src/modules/alerts/application/notification.service.ts` | `NotificationService` + `NodemailerMailSender` + `NodeHttpPoster` |
| `apps/governa-core/src/modules/alerts/infrastructure/prisma-notification-config.repository.ts` | Adapter Prisma para `NotificationConfigRepository` |
| `apps/governa-core/.env.example` | Faltam as vars SMTP (a adicionar nesta sessão) |

---

## Objetivo desta sessão

### Tarefa 1 — Wire `NotificationService` em `server.ts` (obrigatório)

Completar E5.4 tornando o `NotificationService` ativo no bootstrap:

```typescript
// server.ts — imports a adicionar
import { PrismaNotificationConfigRepository } from './modules/alerts/infrastructure/prisma-notification-config.repository'
import { NotificationService, NodemailerMailSender, NodeHttpPoster } from './modules/alerts/application/notification.service'
```

Lógica de bootstrap (ler SMTP do env, instanciar somente se configurado):

```typescript
// Monta NotificationService apenas se SMTP_HOST estiver presente
const notificationService = process.env.SMTP_HOST
  ? new NotificationService(
      new PrismaNotificationConfigRepository(prisma),
      new NodemailerMailSender({
        host:   process.env.SMTP_HOST,
        port:   Number(process.env.SMTP_PORT ?? 587),
        secure: process.env.SMTP_SECURE === 'true',
        user:   process.env.SMTP_USER,
        pass:   process.env.SMTP_PASS,
        from:   process.env.SMTP_FROM ?? 'governa@aicockpit.com.br',
      }),
      new NodeHttpPoster(),
    )
  : undefined

const app = createApp({
  // ... deps existentes ...
  notificationService,
})
```

### Tarefa 2 — Atualizar `.env.example` com vars SMTP

Adicionar bloco SMTP ao `.env.example`:

```env
# --- Notificações (SMTP) ---
# Deixar vazio para desabilitar e-mail. Suporte a Gmail, SendGrid, Mailpit (dev)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=<sendgrid-api-key>
# SMTP_FROM=governa@aicockpit.com.br
```

### Tarefa 3 — Aplicar migration (requer Docker)

Se Docker disponível:

```bash
cd apps/governa-core
docker compose up -d postgres   # ou equivalente
pnpm exec prisma migrate deploy
```

### Tarefa 4 — Avaliar próximo entregável (E3 ou E4)

Após wire do `NotificationService` commitado, E5 está 100% concluído.

Próximo passo: escolher entre:

- **E3 — Runtime do agente âncora** (backend puro, sem Docker além do banco)
  - E3.1: ToolScope consultivo — injeção de tools `read_protheus_pedido` e `read_protheus_cliente` no contexto do agente
  - Pré-requisito: `ANTHROPIC_API_KEY` disponível
  - Alto valor de negócio — é o core do produto

- **E4 — Painel de controle (UI Angular)**
  - E4.1: Design system + tokens (compatível com TOTVS)
  - Pré-requisito: nenhum crítico além do backend E1/E2 já prontos
  - Visibilidade imediata para demo com cliente

**Recomendação:** atacar E3.1 (ToolScope consultivo) — é o diferencial competitivo do produto. E4 pode ser feito em paralelo por outro agente ou em sessões subsequentes após E3.

---

## Sequência de execução sugerida

```
1. Ler este report
2. Abrir apps/governa-core/src/server.ts
3. Adicionar imports de PrismaNotificationConfigRepository + NotificationService
4. Adicionar bloco de bootstrap do NotificationService (opt-in via SMTP_HOST)
5. Passar notificationService para createApp()
6. Atualizar apps/governa-core/.env.example com bloco SMTP comentado
7. pnpm --filter governa-core test  — garantir CI verde (473+ testes)
8. pnpm --filter governa-core build — garantir typecheck limpo
9. Commit: "feat(E5.4): wire NotificationService no bootstrap"
10. (Se Docker disponível) prisma migrate deploy
11. Decisão: iniciar E3.1 nesta mesma sessão ou deixar para 2.47
```

---

## Itens em aberto herdados (não-bloqueantes)

- [ ] `_tmp_*` files e `skills/ziJ8B7ij` no working tree — limpar
- [ ] Testar pipeline OTel → Datadog em runtime (requer stack completo)
- [ ] Aplicar migration: `prisma migrate deploy` (requer Docker)

---

## Decisões relevantes herdadas

- `NotificationService` é opt-in: sem `SMTP_HOST` no env, o serviço não sobe e o router `/notifications` não monta — zero impacto em ambientes sem SMTP
- HMAC-SHA256 no header `X-Governa-Signature` para webhook (decidido na sessão 2.44)
- Secrets SMTP ficam em env vars, nunca em banco

---

## Após esta sessão (2.47+)

- **E3.1** — ToolScope consultivo: montar contexto de tools `read_protheus_pedido` + `read_protheus_cliente` e injetá-las no agente âncora via Claude API
- **E4.1** — Design system Angular: tokens TOTVS + componentes base no `governa-ui`
