# Report — Sessão 2.63 · E7 continuação — auth local + deploy governa-core

**Data:** 2026-07-14
**Status:** 🔴 bloqueada — deploy governa-core falhando no Railway
**Sessão anterior:** [`2026-07-13-sessao-2.62.md`](./2026-07-13-sessao-2.62.md)

## TL;DR

Módulo de auth local implementado e commitado; deploy do governa-core no Railway bloqueado por erro de tipos OTel que persiste apesar da correção (cache de build Railway usando commit antigo).

## O que mudou no código

- `prisma/schema.prisma` — model `User` adicionado (email, passwordHash, role, tenantId)
- `prisma/migrations/20260714015127_add_user_model/` — migration criada e aplicada localmente
- `prisma/seed.ts` — upsert de user admin `fabio@targetflex.com.br` com `crypto.scrypt`
- `src/modules/auth/application/auth.service.ts` — `AuthService.login()` + `hashPassword()` com `crypto.scrypt` nativo
- `src/modules/auth/presentation/auth.router.ts` — `POST /auth/login` pública
- `src/app.ts` — monta `/auth` antes do `tenantMiddleware`; `authService` adicionado ao `AppDependencies`
- `src/server.ts` — instancia `AuthService` no bootstrap
- `src/infra/telemetry/tracer.ts` — substituído `@ts-ignore` por `as any` para fix de tipos OTel
- `apps/governa-ui/src/app/core/auth/auth.service.ts` — login agora chama `coreBaseUrl/auth/login`

## Estado verificado do sistema

- **Testes locais:** 52 suites · 581 testes · build TypeScript OK
- **Banco local:** migration `20260714015127_add_user_model` aplicada
- **Deploy Railway governa-core:** ❌ FAILED — Railway usando cache de build desatualizado
- **Último erro:** `src/infra/telemetry/tracer.ts(43,5): error TS2322` — OTel type mismatch
- **Causa raiz:** Railway build cache (steps 1–9 cached) roda com COPY src/ do commit antigo, não pegando o fix do `as any`
- **Deploy governa-ui:** ✅ SUCCESS (sem alteração)
- **Deploy Postgres:** ✅ SUCCESS

## Decisões tomadas

- **D1** — Auth local no core (não no gateway) — core é dono dos dados de usuário
- **D2** — `crypto.scrypt` nativo Node em vez de `bcryptjs` — sem dep extra, Alpine-safe
- **D3** — UI chama `coreBaseUrl/auth/login` (não mais `gatewayBaseUrl`) — alinhado com a responsabilidade
- **D4** — Token 8h, claims `{ tenantId, userId }` — compatível com `tenantMiddleware` existente
- **D5** — Seed com `SEED_ADMIN_PASSWORD` via env var — senha padrão `Governa@2026!` se não definida
- **D6** — Railway PostgreSQL add-on criado no projeto; `DATABASE_URL = ${{ Postgres.DATABASE_URL }}`

## Itens em aberto (não bloqueantes)

- [ ] Deploy governa-core Railway — bloqueado por cache de build (resolver na próxima sessão)
- [ ] `environment.production.ts` ainda aponta para `api.governa.targetflex.com.br` — precisa URL Railway real
- [ ] Seed não rodou no banco Railway (aguardando core subir)
- [ ] `www.aicockpit.com.br` → 404 (limite free Railway)
- [ ] Login na UI não funcional (core não deployado)

## Próxima sessão (2.64) deve

- **Atacar:** resolver cache de build Railway para governa-core + obter URL pública + atualizar env + seed + testar login
- **Sequência sugerida:**
  1. Verificar se o deploy `76a9a289` (triggado no fim desta sessão com `commitSha: dabaf15`) passou
  2. Se ainda falhar: adicionar `OTEL_SDK_DISABLED=true` nas env vars do Railway para contornar o problema OTel completamente (a inicialização do SDK é protegida por `if (!sdkDisabled)`, não afeta o build mas elimina o caminho de código problemático)
  3. **Atenção:** o erro TS acontece em COMPILE TIME, não runtime — `OTEL_SDK_DISABLED` não ajuda no build. O fix real é o `as any` no `tracer.ts`, mas o Railway não está pegando o commit novo.
  4. Solução definitiva: forçar invalidar cache Docker no Railway (via railway-agent com `--no-cache`) ou fazer uma mudança trivial no Dockerfile para quebrar o cache
  5. Após build OK: `pnpm run db:seed` no Railway (via railway-agent ou CLI)
  6. Pegar URL pública do governa-core (formato: `governa-core-production.up.railway.app`)
  7. Atualizar `apps/governa-ui/src/environments/environment.production.ts` com a URL real
  8. Commit + push → governa-ui rebuild automático
  9. Testar `aicockpit.com.br/login` com `fabio@targetflex.com.br` / `Governa@2026!`
- **Começar lendo:** este report (e SÓ este no boot)
- **Consultar sob demanda:** `apps/governa-core/Dockerfile`, `apps/governa-core/src/infra/telemetry/tracer.ts`
- **Atenção a:** Railway build cache é agressivo — o redeploy pode reusar commit antigo; usar `railway-agent` para deploy com commitSha explícito
- **Não tocar:** governa-ui (estável), Postgres (estável), auth module (implementado corretamente)

## Infraestrutura Railway

| Serviço | ID | Status |
|---|---|---|
| governa-ui | `41be4398` | ✅ SUCCESS |
| Postgres | `f2c3caec` | ✅ SUCCESS |
| governa-core | `f809d029` | ❌ FAILED (build cache) |

Projeto Railway: `06415427-00cd-4d08-8315-593d6572f71b`
Env: `d6903f06-9ae5-423c-9c2d-ab6ef26d9249` (production)

## Commits da sessão

- `dabaf15` — feat(E7): auth local — User model + POST /auth/login no governa-core
