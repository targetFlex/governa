# Report — Sessão 2.21 — 2026-06-04

## TL;DR
Validação da sessão 2.20 confirmada (docker compose up + /health 200). Dockerfile do governa-core criado (multi-stage pnpm, Prisma migrate deploy no entrypoint). Serviço `governa-core` adicionado ao docker-compose.yml com depends_on postgres+governa-gateway (healthy). Stack completa em container pronta para build.

## Estado
Concluída

## O que foi feito
- Executado `validate-2.20.sh` localmente: postgres ✅, governa-gateway healthy ✅, `/health` 200 ✅
- Identificado que `GATEWAY_BASE_URL` estava ausente em `apps/governa-core/.env` (era o aviso do passo [4] truncado) — adicionado `http://localhost:3100`
- Criado `apps/governa-core/Dockerfile` (multi-stage): builder instala all deps + `prisma generate` + `tsc`; runner instala prod deps + copia `.prisma`, `@prisma/client`, `prisma` CLI do builder + dist compilado; CMD: `prisma migrate deploy && node dist/server.js`
- Adicionado serviço `governa-core` em `infra/docker/docker-compose.yml`: porta 3000, `DATABASE_URL` apontando para `postgres:5432` (rede interna), `GATEWAY_BASE_URL` apontando para `governa-gateway:3100`, `depends_on` com `condition: service_healthy` para ambos
- Removido `governa/validate-2.20.sh` (validação concluída, script descartável)

## Decisões tomadas
- **D1** — Prisma CLI copiado explicitamente do builder para runner: `pnpm install --prod` não instala `prisma` (devDep), mas `migrate deploy` é necessário no entrypoint; cópia seletiva evita instalar all deps no runner.
- **D2** — `depends_on: condition: service_healthy` para postgres e governa-gateway: garante que o core não inicia antes do DB e do gateway estarem prontos, evitando falhas de conexão no bootstrap.
- **D3** — `REDIS_URL` injetado no docker-compose mesmo sem uso imediato: BullMQ já está nas deps do core; declarar agora evita retrabalho quando filas forem ativadas.

## Desvios do plano
- Nenhum — escopo do handoff coberto integralmente.

## Handoff — próxima sessão 2.22
Executar `docker compose -f infra/docker/docker-compose.yml build governa-core` e em seguida `up -d postgres governa-gateway governa-core`. Verificar `docker logs governa_core` — deve mostrar `prisma migrate deploy` + `servidor ouvindo na porta 3000`. Se build ou migrate falhar, investigar e corrigir. Se tudo verde: iniciar pipeline CI/CD (GitHub Actions).

## Arquivos modificados
- `apps/governa-core/Dockerfile` — novo (multi-stage, pnpm, Prisma migrate deploy no entrypoint)
- `apps/governa-core/.env` — adicionado `GATEWAY_BASE_URL=http://localhost:3100`
- `infra/docker/docker-compose.yml` — adicionado serviço `governa-core` (porta 3000, depends_on healthy)
- `governa/validate-2.20.sh` — removido (validação concluída)

## Pendências / alertas para sessões futuras
- Build do governa-core ainda não executado — deve ser validado no início da 2.22
- governa-core não tem `.dockerignore`: considerar adicionar (exclui `node_modules`, `dist`, `coverage`, `.env`) para reduzir contexto de build
- CI/CD com GitHub Actions ainda não implementado (próximo ciclo após stack validada)
- Storybook + Axe-core + Playwright E2E ainda pendentes (governa-ui)
- `version: "3.9"` no docker-compose.yml gera warning "obsolete" — pode ser removido em sessão futura
