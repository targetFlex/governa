# Report — Sessão 2.36-autonomia — 2026-06-14

## TL;DR

Protocolo de autonomia total formalizado na skill `governa-agentes-totvs`: todos os pushes, PRs, merges e reviews passam pelo GitHub MCP — zero ações manuais do usuário. Seção 0.5 adicionada com mapeamento completo git → GitHub MCP e fluxo autônomo de sessão de código.

## Estado

Concluída

## O que foi feito

1. **Remoção da confirmação antes de commitar report** (seção 2.2): fluxo agora é escrever → push via MCP → informar hash. Sem "Confirmas?".
2. **Push de report via `mcp__github__push_files`** (seção 2.2): substitui `git push` (SSH bloqueado na sandbox). Zero ação manual.
3. **Fallback documentado** (seção 2.3): se GitHub MCP indisponível → informar o usuário e aguardar restauração. Nunca pedir `git push` manual.
4. **Seção 0.5 — Princípio de Autonomia Total** adicionada:
   - Mapeamento completo: `git push` → `mcp__github__push_files`, `gh pr create` → `mcp__github__create_pull_request`, `gh pr merge` → `mcp__github__merge_pull_request`, etc.
   - Fluxo autônomo de 9 passos: boot → branch → código → testes → push → PR → CI → merge → report.
   - Regra inegociável: ação manual do usuário = falha de protocolo.
5. **allowed-tools expandido**: adicionados `mcp__github__create_or_update_file`, `mcp__github__get_pull_request`, `mcp__github__get_pull_request_status`, `mcp__github__merge_pull_request`, `mcp__github__create_pull_request_review`, `mcp__github__list_commits`, `mcp__github__get_file_contents`, `mcp__github__create_branch`, `mcp__github__search_code`.
6. **SKILL.md e `.skill` pushados via GitHub MCP** (sem git local, sem SSH).

## Decisões tomadas

- **D1** — Seção 0.5 antes da Seção 1: autonomia é pré-condição operacional, não regra de encerramento — posição antes do trabalho garante que o agente internalize antes de começar.
- **D2** — Mapeamento em tabela: formato escaneável; agente encontra o substituto certo sem ambiguidade.
- **D3** — Fallback explícito na seção 0.5: cobre falhas de MCP durante qualquer fase (não só no encerramento).

## Desvios do plano

- Nenhum

## Handoff — próxima sessão 2.37

Verificar CI do PR #12 (`feat/session-2.34-otel` → `main`). Se verde, fazer merge via `mcp__github__merge_pull_request`. Em seguida iniciar **E5.3**: integração automática `AuditService.appendInChain()` → `evaluate(AUDIT_RECORDED)` e `PolicyEngine`/router → `evaluate(TOOL_BLOCKED)`.

## Arquivos modificados

- `governa/skills/governa-agentes-totvs/SKILL.md` — seção 0.5 adicionada, allowed-tools expandido, fluxo encerramento sem confirmação
- `governa/docs/reports/2026-06-14-sessao-2.35-pendencia.md` — report da pendência 2.35 (PR #12)
- `governa/docs/reports/2026-06-14-sessao-2.36-autonomia.md` — este report

## Pendências / alertas para sessões futuras

- Instalar a nova versão da skill via botão "Save skill" no Cowork após esta sessão
- CI do PR #12 pendente de verificação
- E5.3 pendente: integração automática AuditService + PolicyEngine → evaluate()
- GitHub MCP pode requerer reautenticação a cada nova sessão Cowork (token não persistido entre sessões)
