// DEPRECATED — config real está em jest.config.js
// Este arquivo só existe porque o sandbox de execução não permite delete.
// Não importe daqui. Scripts em package.json usam --config jest.config.js.
export {}
